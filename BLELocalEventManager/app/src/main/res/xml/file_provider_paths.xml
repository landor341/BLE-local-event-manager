<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Allows sharing files from the app's internal cache directory.
         Used by TelemetryManager CSV export via FileProvider. -->
    <cache-path
        name="telemetry_exports"
        path="." />
</paths>